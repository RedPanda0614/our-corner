// ===== 只需要改这个文件 =====
// 填上存数据的 GitHub 私有仓库。留空的话，网站只保存在当前这台设备上（本地预览模式）。
window.CC_CONFIG = {
  github: {
    owner: 'RedPanda0614',
    repo: 'our-corner-data',
    branch: 'main',
    pollSeconds: 20     // 每隔多少秒检查一次对方的更新
  }
};
